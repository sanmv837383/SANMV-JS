//string can be declared using both single and double quotes - '' " "
//the concatenation of the string could be done using + Symbol

const name="sanajna"
const repoCount = 50

console.log(name + repoCount + "Value");

console.log(`hello my name is ${name} and my repo count is${repoCount}`);
//here the `` symbol can be used to do the concatenation in it, it is called as string interpolation

console.log("hello");